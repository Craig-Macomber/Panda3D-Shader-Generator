:Info
place   color

:Params
inout float4 color

:Shader
float maxBrightness=2.0;

float4 c=color;
c=c*maxBrightness;

//bloom using brigtness past 1
float b=max(max(c.x,c.y),c.z);
c.w=b/maxBrightness;
c.xyz+=max((b-1)/2,0);

color=c;